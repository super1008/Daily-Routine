# Daily Routine

습관 체크(오늘) · 목표(To-Do) · 통계 · 일기를 관리하는 React 앱입니다.
별도 서버/DB 없이 브라우저의 `localStorage`에 모든 데이터를 저장합니다.

## 실행

```bash
npm install
npm run dev
```

## 빌드

```bash
npm run build
```

데이터는 브라우저 저장소(`localStorage`)에만 남으므로, 브라우저 데이터를 지우거나
다른 브라우저/기기에서 열면 초기 예시 데이터로 리셋됩니다.
